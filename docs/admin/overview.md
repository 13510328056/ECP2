# 管理后台总览

## 访问方式

**地址：** `/admin/login`

**默认管理员账号：**
| 字段 | 值 |
|------|-----|
| 邮箱 | admin@lisindustrial.gh |
| 密码 | admin123 |

## 登录流程

1. 访问 `/admin/login`
2. 输入管理员邮箱和密码
3. 输入图形验证码（4位随机数字）
4. 点击「登录」按钮
5. 验证通过后跳转至数据看板

**登录页面功能：**
- 显示/隐藏密码
- 记住密码选项
- 验证码刷新
- 错误提示
- 登录状态加载动画

## 功能模块

| 模块 | 路由 | 功能描述 |
|------|------|---------|
| 数据看板 | `/admin/dashboard` | 销售统计、热门商品、最新订单 |
| 商品管理 | `/admin/products` | 商品列表、搜索筛选、上下架、批量删除 |
| 商品添加 | `/admin/products/create` | 创建新商品（含多语言、规格、价格阶梯） |
| 商品编辑 | `/admin/products/:id/edit` | 编辑现有商品 |
| 分类管理 | `/admin/categories` | 分类树形管理、排序、启禁用 |
| 订单管理 | `/admin/orders` | 订单列表、状态筛选、搜索 |
| 订单详情 | `/admin/orders/:id` | 订单处理、物流录入、状态操作 |
| 询盘管理 | `/admin/inquiries` | 询盘列表、状态筛选、搜索 |
| 询盘详情 | `/admin/inquiries/:id` | 询盘回复、状态更新 |
| 系统设置 | `/admin/settings` | 个人资料、密码修改、用户管理 |

## 导航布局

后台采用独立布局组件 `AdminLayout.vue`：

```
┌─────────────────────────────────────────┐
│  [Logo]  管理中心   [管理员名称]  [退出]  │  ← 顶部导航
├─────────────────────────────────────────┤
│                                         │
│             内容区域                      │  ← 页面内容
│                                         │
├─────────────────────────────────────────┤
│  仪表盘  商品  订单  询盘  设置            │  ← 底部标签导航
└─────────────────────────────────────────┘
```

## 路由守卫

所有后台路由（除 `/admin/login`）受 `requiresAdmin` 守卫保护：

```typescript
router.beforeEach((to, _from, next) => {
  if (to.meta.requiresAdmin) {
    const adminStore = useAdminStore()
    if (!adminStore.isAuthenticated) {
      next('/admin/login')
      return
    }
  }
  next()
})
```

未登录用户直接访问后台页面将自动重定向到登录页。

## 数据看板

**路由：** `/admin/dashboard`

**统计卡片（4个）：**
| 卡片 | 数据 |
|------|------|
| 今日订单 | 示例：128 |
| 本月销售额 | 示例：₵42,500 |
| 活跃商品 | 示例：1,248 |
| 待处理询盘 | 示例：18 |

**销售趋势图：**
- 7日销售趋势展示
- 柱状图可视化
- 每日对比显示

**最新订单：**
- 最近4笔订单概览
- 显示订单号、客户名、金额、状态

## 覆盖状态

| 功能点 | 状态 |
|--------|------|
| 管理员登录（验证码） | ✅ 已完成 |
| 路由守卫权限控制 | ✅ 已完成 |
| 底部标签导航 | ✅ 已完成 |
| 数据看板统计卡片 | ✅ 已完成 |
| 销售趋势图表 | ✅ 已完成 |
| 最新订单列表 | ✅ 已完成 |
| 管理员退出登录 | ✅ 已完成 |
